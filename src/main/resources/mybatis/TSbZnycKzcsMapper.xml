<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.axy.intelligentcontrolplatform.standing.mapper.TSbZnycKzcsMapper">

    <select id="getPartsParam" resultType="com.axy.intelligentcontrolplatform.standing.entity.TSbZnycKzcs">
        SELECT
        t1.sbmc bjfjName,
        t2.sblx sblxName,
        t.*
        FROM
        t_sb_znyc_kzcs t
        LEFT JOIN t_sb_znyc_bjfj t1 ON t.bjid = t1.obj_id
        LEFT JOIN t_dw_bzzx_sbfl t2 ON t.sblx = t2.sblxbm
        LEFT JOIN t_sb_znyc_kzcs_dy t3 ON t.csdyid = t3.obj_id
        <where>
            <if test="objId != null and objId !=''">
                and t.obj_id= #{objId}
            </if>
            <if test="csz != null and csz !=''">
                and t.csz= #{csz}
            </if>
            <if test="csmc != null and csmc !=''">
                and t.csmc like concat(concat('%',#{csmc}),'%')
            </if>
        </where>
    </select>
    <select id="getPartsParamCount" resultType="java.lang.Integer">
        SELECT
        count(0)
        FROM
        t_sb_znyc_kzcs t
        LEFT JOIN t_sb_znyc_bjfj t1 ON t.bjid = t1.obj_id
        LEFT JOIN t_dw_bzzx_sbfl t2 ON t.sblx = t2.sblxbm
        LEFT JOIN t_sb_znyc_kzcs_dy t3 ON t.csdyid = t3.obj_id
        <where>
            <if test="objId != null and objId !=''">
                and t.obj_id= #{objId}
            </if>
            <if test="csz != null and csz !=''">
                and t.csz= #{csz}
            </if>
            <if test="csmc != null and csmc !=''">
                and t.csmc like concat(concat('%',#{csmc}),'%')
            </if>
        </where>
    </select>
</mapper>
